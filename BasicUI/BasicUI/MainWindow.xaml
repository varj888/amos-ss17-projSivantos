<Window x:Class="BasicUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BasicUI"
        mc:Ignorable="d"
        Title="Connect to device" Height="300" Width="450"
        MaxHeight="300" MaxWidth="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <!--TextBox for entering the IP-Adress of a device-->
        <TextBox Grid.Column="0" Grid.Row="1" ToolTip="Enter IP-Adress here"
                 Margin="28,30.5,10.5,155" FontSize="18"
                 TextAlignment="Center"
                 Text="{Binding IPaddress}"/>
        <!--Button for establishing connection to device-->
        <Button Grid.Column="1" Grid.Row="1" x:Name="connectIP_button"
                Margin="60,30,40,155" Click="connectIP_button_Click">Connect</Button>
    </Grid>
</Window>
